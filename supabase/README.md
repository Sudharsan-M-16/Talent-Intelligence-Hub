# Supabase (supabase/)

This folder will contain:
- SQL migrations (RLS + tenant isolation)
- RLS policies for roles: Admin / Recruiter / Viewer
- Full-text search support using `search_vector TSVECTOR` + triggers
- pg_trgm indexes for fast partial matching
